import 'package:flutter/material.dart';

class ModificatedDivisor extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Divider(
      color: Colors.orange,
    );
  }
}
